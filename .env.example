# PostgreSQL (used by Docker Compose; not needed for local SQLite dev)
POSTGRES_USER=wikiapp
POSTGRES_PASSWORD=wikiapp
POSTGRES_DB=museums
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Or set DATABASE_URL directly:
# DATABASE_URL=postgresql+psycopg2://wikiapp:wikiapp@localhost:5432/museums

# Model artifacts directory
ARTIFACTS_DIR=./artifacts

# Wikipedia API user agent (set a real contact for production)
WIKIPEDIA_USER_AGENT=wikiapp/0.3 (museum-data-pipeline)
